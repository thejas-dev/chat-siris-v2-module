# bullmq declares `peerOptional redis@>=5`, but this service uses redis@4 (node-redis).
# bullmq uses ioredis internally, so this peer is only a hint. Relax peer resolution
# so clean installs (e.g. Render) don't fail with ERESOLVE.
legacy-peer-deps=true
